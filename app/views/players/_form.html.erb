<%= form_with model: @player, local:true do |form| %>

  <% if @player.errors.any? %>
  <div>
    <h3>
      <%= pluralize(@player.errors.count, 'error') %> prohibited this map from being saved:
    </h3>

    <ul>
    <% @player.errors.full_messages.each do |error| %>
      <li><%= error %></li>
    <% end %>
    </ul>
  </div>
  <% end %>

  <p>
    <%= form.label :name %><br>
    <%= form.text_field :name %>
  </p>

  <p>
    <%= form.label :age %><br>
    <%= form.number_field :age, in: 1..99%>
  </p>

  <p>
    <%= form.submit %>
  </p>
<% end %>
